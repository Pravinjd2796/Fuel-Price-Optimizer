# ============================================================================
# Fuel Price Optimizer - Requirements
# ============================================================================
# Install with: pip install -r requirements.txt
# ============================================================================

# ----------------------------------------------------------------------------
# Core ML and Data Processing
# ----------------------------------------------------------------------------
pandas>=1.5.0,<3.0.0
numpy>=1.24.0,<2.0.0
scikit-learn>=1.2.0,<2.0.0
xgboost>=1.7.0,<3.0.0
joblib>=1.2.0,<2.0.0
matplotlib>=3.5.0,<4.0.0
statsmodels>=0.13.0,<1.0.0
python-dateutil>=2.8.0,<3.0.0

# ----------------------------------------------------------------------------
# API and Web Framework
# ----------------------------------------------------------------------------
fastapi>=0.95.0,<1.0.0
uvicorn[standard]>=0.22.0,<1.0.0
pydantic>=1.10.0,<2.0.0

# ----------------------------------------------------------------------------
# Testing Framework
# ----------------------------------------------------------------------------
pytest>=7.0.0,<8.0.0
pytest-cov>=4.0.0,<5.0.0

# ----------------------------------------------------------------------------
# Development Tools (Required for CI/CD)
# ----------------------------------------------------------------------------
black>=23.0.0,<24.0.0
flake8>=6.0.0,<7.0.0
mypy>=1.0.0,<2.0.0
isort>=5.12.0,<6.0.0

# ============================================================================
# Optional Dependencies (Uncomment as needed)
# ============================================================================

# ----------------------------------------------------------------------------
# Scheduling Options (Choose one based on your needs)
# ----------------------------------------------------------------------------

# Option 1: APScheduler - Simple Python-based scheduling (Recommended for development)
# apscheduler>=3.10.0,<4.0.0

# Option 2: Prefect - Modern workflow orchestration (Recommended for production)
# prefect>=2.10.0,<3.0.0

# Option 3: Apache Airflow - Enterprise-grade workflow orchestration
# Note: Airflow has complex dependencies and should be installed separately
# See: https://airflow.apache.org/docs/apache-airflow/stable/installation/index.html
# apache-airflow>=2.7.0,<3.0.0

# ----------------------------------------------------------------------------
# Additional Utilities (Uncomment if needed)
# ----------------------------------------------------------------------------

# HTTP requests (if needed for external APIs)
# requests>=2.28.0,<3.0.0

# Environment variable management
# python-dotenv>=1.0.0,<2.0.0

# Code formatting and linting (already included in dev tools above)
# Can be installed separately if needed: pip install black flake8 mypy isort

# ============================================================================
# Installation Notes
# ============================================================================
# 
# Basic installation:
#   pip install -r requirements.txt
#
# For development with all optional tools:
#   pip install -r requirements.txt
#   pip install apscheduler  # or prefect, or apache-airflow
#
# For scheduling only:
#   pip install -r requirements.txt
#   pip install apscheduler  # Choose one: apscheduler, prefect, or apache-airflow
#
# ============================================================================
